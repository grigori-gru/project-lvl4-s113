extends ../layouts/layout.pug

block content
  .row.mt-3
     .col-sm-6.mx-auto
       h3.text-xs-center Edit '#{task.name}'
       form(action=`/tasks/${task.id}?_method=PATCH` method='post')
         include _form.pug
