extends ../layouts/layout.pug

block content
  .row.mt-3
    .col-sm-6.mx-auto
      h3.text-xs-center Sign in
      +formFor(f, { url: urlFor('sessions'), method: 'post' })
        +input(f, 'email')
        +input(f, 'password', { as: 'password' })
        +button(f)
    
